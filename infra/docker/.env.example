# Backend runtime
BACKEND_PORT=3000
PORT=3000
HOST=0.0.0.0
STORAGE_MODE=mongo-minio
INGEST_ROOT_DIR=/workspace/apps/backend/generated/inbox
CORS_ORIGIN=

# MongoDB
MONGO_INITDB_ROOT_USERNAME=cuchen
MONGO_INITDB_ROOT_PASSWORD=change-me
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DATABASE_NAME=cuchen
MONGODB_USER=cuchen
MONGODB_PASSWORD=change-me
MONGO_COLLECTION_NAME=catalog

# MinIO
MINIO_ROOT_USER=cuchen
MINIO_ROOT_PASSWORD=change-me
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=cuchen
MINIO_SECRET_KEY=change-me
MINIO_BUCKET=cuchen-images
